module.exports = async function (msg) {
    /* if ((msg.content).toLowerCase() === "!imgur") {

        const characters = "abcdefghijklmnopqrstuvwxyz0123456789";
        let array = [];
        for(let i = 0; i < 7; i++) {
            let ind = characters[Math.floor(Math.random() * characters.length)];
            array.push(ind);
        }

        for (let i = 0; i < array.length; i++) {
            let ind = Math.round(Math.random() * 1);
            if (ind === 1) {
                array[i] = array[i].toUpperCase();
            }
        }
        let query = array.join("");
        
        msg.channel.send(`https://imgur.com/gallery/${query}`);
        console.log(`https://imgur.com/gallery/${query}`);
    } */
}